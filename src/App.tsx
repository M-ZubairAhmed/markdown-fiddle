import * as React from 'react';

export default class App extends React.Component {
  state = {
    inputText: '',
  };

  render() {
    return (
      <div>
        <h1>Welcome to React</h1>
      </div>
    );
  }
}
